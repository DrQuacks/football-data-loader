# Fantasy Football Data Loader

A Node.js + TypeScript tool to load Sports Reference CSV data into a PostgreSQL database.

## Usage

Run loader with:
npx ts-node scripts/loadReceivingCsv.ts

Run PostGres with:
psql fantasy_football